services:
    redis:
        image: redis:latest
        container_name: redis-server
        ports:
            - "6379:6379"
        command: redis-server --requirepass test123
        volumes:
            - redis_data:/data
        restart: unless-stopped
volumes:
  redis_data: